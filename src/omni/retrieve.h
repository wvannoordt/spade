#pragma once

namespace spade::omni
{
    template <typename index_t, typename array_t, typename info_t>
    auto retrieve(const index_t& i, const array_t& array, const info_t&)
    {
        
    }
}