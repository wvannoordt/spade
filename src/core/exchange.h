#pragma once

#include "core/grid.h"
#include "core/grid_array.h"

namespace spade::parallel
{
    //todo: return a handle that can be awaited
    template <typename array_t> void exchange_array(array_t& arr)
    {
        
    }
}